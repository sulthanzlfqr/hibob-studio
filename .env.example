# Hibob Studio — Environment Variables
# Salin file ini ke .env dan isi nilai yang sesuai.

# URL endpoint order API (Cloudflare Pages Function).
# Dalam produksi: /api/order (relative, same domain)
# Dalam development local: http://localhost:8788/api/order (wrangler pages dev)
VITE_ORDER_API_URL=/api/order
